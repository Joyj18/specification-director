[project]
name = "specification-director"
authors = ["Jane Doe"]
description = "A platform for creating and managing specification-driven NFTs with unique mathematical properties"
telemetry = false
requirements = []
[contracts.specification-director]
path = "contracts/specification-director.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
